# GPS Camera v1.0
Android camera with selectable coordinate, elevation, GPS accuracy, date and time overlays. The overlay is permanently rendered into the JPEG. GPS position, altitude and capture time are also written to EXIF.

Photos are saved in `Pictures/GPS Camera`.

The included GitHub Actions workflow builds a debug-signed installable APK.
